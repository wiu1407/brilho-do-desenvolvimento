const PatternBreak = () => {
  return (
    <section className="bg-primary px-4 py-12">
      <div className="container mx-auto max-w-3xl text-center">
        <h2 className="text-2xl md:text-3xl font-extrabold text-primary-foreground">
          ✨ Mas existe uma forma prática de começar a mudar isso ainda hoje
        </h2>
      </div>
    </section>
  );
};

export default PatternBreak;
