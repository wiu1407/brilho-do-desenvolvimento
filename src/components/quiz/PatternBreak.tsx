const PatternBreak = () => {
  return (
    <section className="gradient-cool px-4 py-12">
      <div className="container mx-auto max-w-lg text-center">
        <h2 className="text-xl sm:text-2xl md:text-3xl font-extrabold text-primary-foreground">
          ✨ Mas existe uma forma prática de começar a mudar isso ainda hoje
        </h2>
      </div>
    </section>
  );
};

export default PatternBreak;
